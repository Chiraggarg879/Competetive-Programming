#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);

void solve(vector<int>& seats, string& str, int fill, int empty) {
     // Your solution here
     
}

int main() {
    fastio;
    int t = 1;
    cin >> t;
    while (t--) {
        int n,x,s;
        string str;
        cin>>n>>x>>s;
        cin>>str;
        vector<int> seats(s,0);
        int fill = -1, empty = 0;

        return solve(seats,str,0,fill,empty);
    }
    return 0;
}